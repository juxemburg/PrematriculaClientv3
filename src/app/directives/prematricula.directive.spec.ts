import { PrematriculaDirective } from './prematricula.directive';

describe('PrematriculaDirective', () => {
  it('should create an instance', () => {
    const directive = new PrematriculaDirective();
    expect(directive).toBeTruthy();
  });
});
